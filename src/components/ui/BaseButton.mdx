import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as BaseButtonStories from './BaseButton.stories'

<Meta title="components/ui" />

# Base building blocks for UI

Buttons, Inputs etc.

<Canvas of={BaseButtonStories.Default} />
